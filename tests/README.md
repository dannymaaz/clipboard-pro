# Tests

Planned coverage:

- Rust unit tests for domain rules and SQLite repository behavior.
- Frontend tests for filtering and rendering states.
- Tauri integration tests for command contracts.
- E2E smoke tests for window opening, search and copy.

The MVP scaffold keeps this folder ready without introducing a test dependency before the first implementation cycle needs it.
